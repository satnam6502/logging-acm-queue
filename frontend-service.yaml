apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: satserver
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: satserver
    tier: frontend
